// Find this block in ChallengesScreen.js (around line 295)
// and remove it completely:

{earned && name === 'Micro-Saver' && (
  <View style={styles.featuredBadgeIndicator}>
    <Text style={styles.featuredBadgeText}>New!</Text>
  </View>
)}
