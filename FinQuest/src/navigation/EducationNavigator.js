// This is a no-op module that replaces the removed Education feature
// It exists only to satisfy the bundler's dependency resolution
// without importing any unnecessary components

// Empty component that does nothing
const EducationNavigator = () => null;

export default EducationNavigator;
